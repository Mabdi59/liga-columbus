import styles from './MatchView.module.css';

export default function MatchView() {
  return (
    <div className={styles.container}>
      <h1>Matches</h1>
      <p>Live scores, match details, and results for each game.</p>
    </div>
  );
}
